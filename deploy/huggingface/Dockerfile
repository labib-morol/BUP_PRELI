# Single-image Space: pulls the published fallback image instead of building the
# application source, so the Space repository contains no source code.
# Replace the image reference with the tag actually pushed to Docker Hub.
FROM <dockerhub-user>/gridwise-llm:1.0.0
